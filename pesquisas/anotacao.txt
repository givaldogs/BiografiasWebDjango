super usuario
--------------
usuario = gival
senha = 123456
---------------------------
https://docs.djangoproject.com/pt-br/5.2/intro/overview/

-------------------------------------------
from django.db import models


class Article(models.Model):
    pub_date = models.DateField()
    headline = models.CharField(max_length=200)
    content = models.TextField()
    reporter = models.ForeignKey(Reporter, on_delete=models.CASCADE)
------------------------------------------------------------------
Excelente pergunta 👏 — e muito boa observação!

Tanto o seu código quanto o meu exemplo **funcionam perfeitamente**, mas usam **duas formas diferentes de registrar modelos no Django Admin**.
Vamos ver as diferenças e quando vale a pena usar cada uma 👇

---

## 🧩 O seu código (forma **básica e direta**)

```python
from django.contrib import admin
from .models import *

admin.site.register(Pessoa)
```

➡️ Essa é a **forma simples** de registrar um modelo no admin.
Você está dizendo apenas:

> “Django, registre o modelo `Pessoa` no painel admin com as configurações padrão.”

O Django então cria uma interface administrativa **automática**, sem personalizações (campos padrão, listagem simples etc.).

---

## ⚙️ O meu código (forma **com classe personalizada**)

```python
@admin.register(Reporter)
class ReporterAdmin(admin.ModelAdmin):
    list_display = ('name',)
    inlines = [ArticleInline]
```

➡️ Essa é uma **forma mais avançada**, que te permite **personalizar o comportamento e a aparência** do admin.
Aqui:

* `@admin.register(Reporter)` é um **decorator**, uma forma mais “pythônica” de registrar o modelo.
* `ReporterAdmin` é uma **classe** que herda de `admin.ModelAdmin`.
* Dentro dela, você pode configurar várias opções (ex: colunas, filtros, busca, relacionamentos, etc).

---

### 🔍 Essas duas linhas fazem a mesma coisa:

```python
# Forma 1 — Simples
admin.site.register(Pessoa)

# Forma 2 — Com classe personalizada
@admin.register(Pessoa)
class PessoaAdmin(admin.ModelAdmin):
    pass
```

Ambas **registram o modelo `Pessoa` no admin**, mas a segunda te dá espaço para configurar.

---

## 💡 Exemplo: melhorando seu admin

Você pode deixar o admin da sua app `biografias` mais bonito e funcional assim:

```python
from django.contrib import admin
from .models import Pessoa

@admin.register(Pessoa)
class PessoaAdmin(admin.ModelAdmin):
    list_display = ('nome', 'data_criacao', 'data_modificaca')
    search_fields = ('nome',)
    list_filter = ('data_criacao',)
    ordering = ('-data_criacao',)
```

Com isso, no painel admin:

* Você vê as colunas “nome”, “data_criacao” e “data_modificaca”.
* Pode buscar pessoas pelo nome.
* Pode filtrar por data de criação.
* E a listagem vem ordenada da mais recente para a mais antiga.

---

### ✅ Resumo

| Modo                      | Código                                                              | Quando usar                                                   |
| ------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------- |
| **Simples**               | `admin.site.register(Pessoa)`                                       | Quando não precisa de personalizações                         |
| **Avançado (com classe)** | `@admin.register(Pessoa)\nclass PessoaAdmin(admin.ModelAdmin): ...` | Quando quer configurar colunas, filtros, buscas, inlines, etc |

---

Quer que eu te mostre um exemplo completo de como deixar o **admin da Pessoa bem estilizado**, com **imagem da foto aparecendo na listagem** usando o campo `url_foto`? (fica muito legal 👀)
=====================================================================

meu_app/
├── templates/
│   └── meu_app/
│       └── index.html
├── views.py
├── models.py
└── ...

